# نطاق تقرير الأرباح — المحطة1 / جزء من S1-06

## التغيير

أصبحت مصادر الحركة في `ProfitReportsBloc` مقيدة بهوية موقع المستند: المبيعات، والمرتجعات المرتبطة، ومرتجعات التسوية. يشمل ذلك التجميع حسب الصنف والتصنيف والعميل والفاتورة، وإجمالي الضريبة والإيراد والتكلفة والخصم والربح. أضيفت تبعيات مواقع المستندات والسياق للتحديث الحي.

لم تتغير معادلات التكلفة المجمدة أو خصومات البنود والضرائب. يُنسب كل مرتجع إلى موقعه وتاريخه؛ الرجوع إلى فاتورته الأصلية لقراءة العميل أو التكلفة لا يحولها إلى حركة جديدة في التقرير. لذلك ربط بيانات الفاتورة الأصلية لا يُرشح بموقعها، بينما مصدر حركة البيع نفسه مُرشح.

يبقى تاريخ المخزن المعطل قابلًا للعرض، ولا يتسبب التعطيل في إخفاء أرباح أو خسائر مسجلة.

## الاختبارات

7 اختبارات على قاعدة فعلية في الذاكرة:

- بيع ومرتجع مرتبط ومرتجع تسوية مع ضريبة وخصومات وتكاليف مجمدة تختلف عن التكلفة الحالية؛ مطابقة الإجمالي وتفاصيل الصنف والتصنيف والعميل.
- إضافة نسخة أخرى من المستندات نفسها في مخزن آخر لا تضاعف النتائج.
- ثلاث حالات لاستبعاد كل نوع مستند على حدة مع تطابق جميع تفاصيل التقرير.
- تعطيل المخزن لا يحذف التاريخ المالي.
- تغيير موقع المستند وحده يحدث التقرير تلقائيًا.

إعادة نسبة الموقع تُجرى في تجهيز الاختبار فقط بعد تعطيل قيد عدم تعديل الهوية، لمحاكاة تاريخ مواقع متعددة. لم يُفتح تغيير مواقع المستندات للمستخدمين.

## الملفات

- `lib/features/reports/presentation/bloc/profit_reports_bloc.dart`
- `test/features/business/warehouse_profit_scope_test.dart`
- هذا التقرير ومصفوفة `REPORT_SCOPE_MATRIX_AR.md` وتحديث قائمة المحطة وخارطة الطريق والخطة الأصلية.

## الحدود

S1-06 منجز جزئيًا؛ بقية التقارير وقراءات التاريخ والتصدير وLAN محددة في المصفوفة. يبقى عدد البنود الرئيسية المغلقة5 من7 في المحطة1. لا تغيير للمخطط أو الاشتراكات أو الواجهات. لا بناء في هذه الدفعة؛ Linux وAndroid عند نهاية المحطة وWeb في المحطة النهائية.

## نتائج التحقق النهائية — 21 سبتمبر2026

- الاختبارات الجديدة:7 ناجحة.
- مجموعة التقارير والفروع والتكامل:1397 اختبارًا ناجحًا.
- `flutter analyze`: لا مشكلات.
- `git diff --check` لملف التنفيذ: دون أخطاء.
- لا بناء جديد؛ البناء عند نهاية المحطة وفق الاتفاق.
