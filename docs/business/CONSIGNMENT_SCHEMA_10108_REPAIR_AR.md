# إصلاح مخطط الأمانة 10108 — 24 سبتمبر 2026

## العيب

بعض نسخ Android وصلت إلى user_version=10107 بينما بقي جدول
consignment_inventory_layers ببنية ما قبل عهدة التحويل: لا يحتوي
origin_layer_id وtransfer_allocation_id، ويحتفظ بقيد فريد قديم على
receipt_item_id. كما غابت جداول الإرسال والتخصيص والاستلام الجزئي. لذلك
فشل إلغاء إذن الاستلام عند أول استعلام Drift للطبقة برسالة
no such column: consignment_inventory_layers.origin_layer_id.

## الإصلاح

المخطط 10108 ترقية إصلاحية لا تضيف نموذج أعمال جديد. عند الترقية من إصدار
أقدم تفحص البنية الفعلية بدل الاعتماد على رقم user_version وحده. إذا وجدت
الحالة الناقصة فإنها داخل معاملة واحدة:

1. تزيل حواجز التحويل والطبقات القديمة مؤقتًا.
2. تعيد بناء رأسي التحويل وطبقات الأمانة بالتعريف الحالي مع حفظ الصفوف.
3. تنشئ جداول الإرسال والتخصيص والاستلام الناقصة بترتيب الاعتماد.
4. تعيد تثبيت حواجز التحويل وطبقات الأمانة.
5. لا تعدل كميات أو قيم أو قيودًا مالية أو حالات مستندات الأعمال.

مخططا Drift 10107 و10108 متطابقان وظيفيًا؛ اختلاف الإصدار موجود لإجبار
الأجهزة المتأثرة على تنفيذ إصلاح البنية.

## التحقق

- اختبار ترقية مولّد يحاكي قاعدة موسومة 10107 تحمل بنية 10105: ناجح.
- ترقية نسخة منفصلة من قاعدة الهاتف: بقيت أعداد صفوف كل الجداول القائمة كما
  هي، وبقي صف طبقة الأمانة مطابقًا قبل وبعد، ونجح integrity_check ولم تظهر
  مخالفات في foreign_key_check.
- 51 اختبارًا للأمانة والتحويلات وهوية المورد: ناجحة.
- تحليل Flutter الكامل: بلا ملاحظات.
- APK التجريبي بُني وثُبّت مع الاحتفاظ بالبيانات، وفتح القاعدة بنجاح. كما نجح بناء Android App Bundle Release وLinux Release.
- المقارنة بعد تثبيت APK لم تسجل أي اختلاف في أعداد صفوف الجداول.
- إذن الهاتف CR-10111563 أصبح voided، وطبقته أصبحت voided بكمية متبقية
  صفر، ولم يبق رصيد supplier_owned_quantity تابع له.

النسخة الاحتياطية السابقة للتثبيت موجودة في:
`/tmp/tapix_device_backup_20260924_10108_before/`.
