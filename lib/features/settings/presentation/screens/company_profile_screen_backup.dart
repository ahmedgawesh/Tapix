import 'package:flutter/material.dart';

class CompanyProfileScreenBackup extends StatelessWidget {
  const CompanyProfileScreenBackup({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
